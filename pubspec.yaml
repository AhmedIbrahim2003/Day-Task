name: day_task
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.7
  hexcolor: ^3.0.1

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/applogo.svg

  fonts:
    - family: Pilat Extended
      fonts:
        - asset: assets\fonts\pilat extended\PilatExtended-SemiBold.ttf
        - weight: 600
